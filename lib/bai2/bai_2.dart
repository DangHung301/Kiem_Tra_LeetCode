class Solution {
  String addStrings(String num1, String num2) {
    int i = num1.length - 1;
    int j = num2.length - 1;
    int carry = 0;
    StringBuffer sb = new StringBuffer();
    while (i >= 0 || j >= 0 || carry > 0) {}
  }
}
